# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'app-event' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for app-event
pod 'Alamofire' 

  target 'app-eventTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'app-eventUITests' do
    # Pods for testing
  end

end
